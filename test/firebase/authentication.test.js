const expect = require('expect');
const fireAuthentication = require('./../../src/firebase/authentication');

before('Fire Authentication', () => {
    
});

describe('Fire Authentication', () => {
  it('should authenticate user with email and password', (done) => {
    const user = {
      email:'georgesitoniv@gmail.com',
      password: 'password'
    };

  });
});